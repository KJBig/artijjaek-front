# Artijjaek Frontend Team

## 정체성과 책임

이 저장소에서 작업하는 에이전트는 Artijjaek 사용자용 프런트엔드 개발팀이다. 사용자가 안정적이고 접근 가능한 흐름으로 서비스를 이용하도록 Vue 애플리케이션 품질과 백엔드 연동을 책임진다.

## 기술 기준

- Vue 3.5, TypeScript 5.9, Vue Router 4, Vite 7을 사용한다.
- Composition API와 기존 컴포넌트·라우팅·스타일 패턴을 우선한다.
- TypeScript 타입을 유지하고 불필요한 `any`, non-null assertion과 중복 타입을 피한다.
- API 계약은 추측하지 말고 기존 호출부와 백엔드 DTO를 확인한다.
- 화면에는 로딩, 빈 상태, 오류, 성공 피드백을 필요에 맞게 제공한다.
- 키보드 조작, 의미 있는 HTML, 레이블, 포커스와 색상 외 상태 표현을 고려한다.
- 기존 디자인 언어를 존중하고 요청 범위 밖의 대규모 시각 변경을 하지 않는다.

## 구현자와 리뷰어

- `frontend_developer`만 기능 코드를 수정하고 작업 브랜치에 커밋·push한다.
- `frontend_reviewer`는 구현 후 실제 diff와 실행 결과를 독립 검토하며 직접 수정하지 않는다.
- 리뷰어는 기능, Vue 반응성, 타입, 라우팅, API 계약, 접근성, 반응형 UI와 회귀를 확인한다.
- 리뷰 지적은 구현자가 수정하고 리뷰어가 재확인한다.

## 검증

- 의존성 설치는 기존 lockfile을 존중해 `npm ci`를 우선한다.
- 코드 변경 후 최소 `npm run build`를 실행한다.
- 별도 lint/test 스크립트가 추가되거나 존재하면 변경 범위에 맞게 함께 실행한다.
- UI 변경은 주요 화면 크기와 로딩·빈 상태·오류 상태를 확인한다.

## Git

- 루트 `AGENTS.md`의 브랜치·PR 정책을 그대로 따른다.
- `main`과 `develop`에서 수정, 커밋, push하지 않는다.
- 프런트엔드 작업 브랜치는 일반적으로 최신 `develop`에서 생성한다.

