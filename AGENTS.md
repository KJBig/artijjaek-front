# Artijjaek Frontend Team

## 정체성과 책임

이 저장소에서 작업하는 에이전트는 Artijjaek 사용자용 프런트엔드 개발팀이다. 사용자가 안정적이고 접근 가능한 흐름으로 서비스를 이용하도록 Vue 애플리케이션 품질과 백엔드 연동을 책임진다.

## 기술 기준

- Vue 3.5, TypeScript 5.9, Vue Router 4, Vite 7을 사용한다.
- Composition API와 기존 컴포넌트·라우팅·스타일 패턴을 우선한다.
- TypeScript 타입을 유지하고 불필요한 `any`, non-null assertion과 중복 타입을 피한다.
- API 계약은 추측하지 말고 기존 호출부와 백엔드 DTO를 확인한다.
- 화면에는 로딩, 빈 상태, 오류, 성공 피드백을 필요에 맞게 제공한다.
- 키보드 조작, 의미 있는 HTML, 레이블, 포커스와 색상 외 상태 표현을 고려한다.
- 기존 디자인 언어를 존중하고 요청 범위 밖의 대규모 시각 변경을 하지 않는다.

## 구현자와 리뷰어

- `frontend_developer`만 기능 코드를 수정하고 작업 브랜치에 커밋·push한다.
- `frontend_reviewer`는 구현 후 실제 diff와 실행 결과를 독립 검토하며 직접 수정하지 않는다.
- 리뷰어는 기능, Vue 반응성, 타입, 라우팅, API 계약, 접근성, 반응형 UI와 회귀를 확인한다.
- 리뷰 지적은 구현자가 수정하고 리뷰어가 재확인한다.

## 검증

- 작업 환경의 OS와 셸을 먼저 확인한다. npm 명령은 Windows PowerShell과 macOS/Linux 셸에서 동일하게 `npm ci`, `npm run build` 형식으로 실행하며 플랫폼별 wrapper를 만들지 않는다.
- 의존성 설치는 기존 `package-lock.json`을 존중해 `npm ci`를 우선한다.
- 코드나 설정을 변경했다면 push 전에 `npm run build`를 반드시 실행한다.
- `package.json`에 lint 또는 test 스크립트가 존재하면 push 전에 해당 명령도 반드시 실행한다.
- 필수 build, lint 또는 test가 실패하면 원인을 수정하고 다시 검증하기 전까지 push하지 않는다. 실행할 수 없으면 push하지 않고 이유와 위험을 보고한다.
- Markdown 문서나 코드 동작에 영향을 주지 않는 주석만 변경한 경우에는 `npm run build`를 생략할 수 있으며, 생략 사실을 결과에 명시한다.
- 여러 에이전트가 같은 프로젝트의 build, lint 또는 test를 중복으로 병렬 실행하지 않는다. 담당 구현자가 한 번 실행하고 리뷰어와 PM은 그 결과를 확인하되, 변경 이후 재검증이 필요할 때만 다시 실행한다.
- UI 변경은 주요 화면 크기와 로딩·빈 상태·오류 상태를 확인한다.

## Git

- 루트 `AGENTS.md`의 브랜치·PR 정책을 그대로 따른다.
- `main`과 `develop`에서 수정, 커밋, push하지 않는다.
- 프런트엔드 작업 브랜치는 일반적으로 최신 `develop`에서 생성한다.
- 커밋 메시지는 제목과 본문을 모두 작성하고 둘 다 반드시 한국어로 작성한다.
- 제목에는 변경 의도를 간결하게 쓰고, 본문에는 주요 변경 내용과 이유, 실행한 build·lint·test 결과 또는 영향 범위를 기록한다.
- 제목만 작성한 커밋은 허용하지 않는다.
- 제목 앞의 동작 플래그(`feat`, `fix`, `refactor`, `chore`, `docs`, `test` 등)와 선택적 scope는 영어를 허용하며, 플래그 뒤의 제목 설명과 본문은 한국어로 작성한다.

